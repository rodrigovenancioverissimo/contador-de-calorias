<h1>Novo Usuário</h1>

<%= form_for(@user, method: :post) do |f| %>
  <%= render 'shared/validates_errors', obj: @user %>
  <%= render 'form_fields', f: f %>

  <div class="actions mb-3">
    <%= f.submit 'Criar', class: 'btn btn-success' %>
  </div>
<% end %>


<%= link_to 'Voltar', :back, class: 'btn btn-primary' %>